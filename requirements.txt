# --- Core Python packages ---
requests==2.32.5
pandas==2.3.3
pyodbc==5.1.0 # optional sql server driver
pypyodbc==1.3.6 # main sql server driver

# --- dbt core + SQL Server adapter ---
dbt-core
dbt-sqlserver

# --- Orchestration (Prefect) ---
prefect==3.5.0

# --- Optional utilities / future use ---
python-dotenv
sqlalchemy
